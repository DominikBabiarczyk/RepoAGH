library(readxl)
# Przykładowa ścieżka do pliku Excela
sciezka_do_pliku <- "beauty.csv"
# Zaimportuj dane z arkusza "Sheet1"
data <- read.csv(sciezka_do_pliku)
View(data)
data$class <- NA
sameCalss <- data[,18:48];
for (i in 1:nrow(data)){
col_index <- which(data[i,18:47] == 1)
if(length(col_index) == 1){
data$class[i] = col_index
}else if(length(col_index) > 1){
data$class[i] = "more 1"
}
}
data <- data[,-c(18:47)]
View(data)
# Przykładowe dane
oceny_1 <- c(4, 5, 6, 7, 8)
oceny_2 <- c(5, 6, 7, 8, 9)
# Przeprowadź test korelacji Pearsona
test_korelacji <- cor.test(oceny_1, oceny_2, method = "pearson")
# Wydrukuj wyniki
print(test_korelacji)
# Przykładowe dane
oceny_1 <- c(4, 5, 7, 7, 8)
oceny_2 <- c(5, 6, 7, 8, 9)
# Przeprowadź test korelacji Pearsona
test_korelacji <- cor.test(oceny_1, oceny_2, method = "pearson")
# Wydrukuj wyniki
print(test_korelacji)
library(readxl)
# Przykładowa ścieżka do pliku Excela
sciezka_do_pliku <- "beauty.csv"
# Zaimportuj dane z arkusza "Sheet1"
data <- read.csv(sciezka_do_pliku)
data$class <- NA
sameCalss <- data[,18:48];
for (i in 1:nrow(data)){
col_index <- which(data[i,18:47] == 1)
if(length(col_index) == 1){
data$class[i] = col_index
}else if(length(col_index) > 1){
data$class[i] = "more 1"
}
}
data <- data[,-c(18:47)]
# Przykładowe dane
courseRating <- data$courseevaluation
profRating <- data$profevaluation
# Przeprowadź test korelacji Pearsona
test_korelacji <- cor.test(courseRating, profRating, method = "pearson")
# Wydrukuj wyniki
print(test_korelacji)
# Przykładowe dane
courseRating <- data$courseevaluation
profRating <- data$profevaluation
# Przeprowadź test korelacji Pearsona
test_korelacji <- cor.test(courseRating, profRating, method = "pearson")
# Wydrukuj wyniki
print(test_korelacji)
diverentialRating <- courseRating - profRating
# Przykładowe dane
courseRating <- data$courseevaluation
profRating <- data$profevaluation
# Przeprowadź test korelacji Pearsona
test_korelacji <- cor.test(courseRating, profRating, method = "pearson")
# Wydrukuj wyniki
print(test_korelacji)
diverentialRating <- abs(courseRating - profRating)
# Przykładowe dane
courseRating <- data$courseevaluation
profRating <- data$profevaluation
# Przeprowadź test korelacji Pearsona
test_korelacji <- cor.test(courseRating, profRating, method = "pearson")
# Wydrukuj wyniki
print(test_korelacji$)
# Przykładowe dane
courseRating <- data$courseevaluation
profRating <- data$profevaluation
# Przeprowadź test korelacji Pearsona
test_korelacji <- cor.test(courseRating, profRating, method = "pearson")
# Wydrukuj wyniki
print(test_korelacji)
diverentialRating <- abs(courseRating - profRating)
print(mean(diverentialRating))
